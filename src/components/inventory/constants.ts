// Re-export from centralized constants for backward compatibility
export {
  INVENTORY_CATEGORIES as categories,
  TOUCH_PANEL_TYPES as touchPanelTypes,
  TOUCH_PANEL_MODULES as touchPanelModules,
  TOUCH_PANEL_CHANNEL_OPTIONS as touchPanelChannelOptions,
  getCategoryColor,
  DEFAULT_INVENTORY_PRICES as defaultPrices
} from '@/constants/inventory'
